# Tuples can be created with and without parenthesis

# myTuple = "a","b","c" and myTuple = ("a","b","c") is actually the same

# Lists can have duplicates whereas tuples cannot
# Lists are mutable whereas tuples are immutable

myTuple_1 = "a", "b", "c"
myTuple_2 = ("a", "b", "c")

print(myTuple_1)
print(myTuple_2)